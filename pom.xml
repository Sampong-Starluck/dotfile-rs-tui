<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.sampong.dotfile</groupId>
  <artifactId>dotfile-java-tui</artifactId>
  <version>0.1.0</version>
  <name>dotfile-java-tui</name>
  <description>Lazygit-style TUI wrapper for package managers (Java port of dotfile-rs-tui)</description>

  <properties>
    <java.version>25</java.version>
    <tamboui.version>0.4.0</tamboui.version>
    <!-- PGO knobs for the "native" profile below — overridden via -D for the instrument/apply steps
         (see mise.toml's native-instrument/native-pgo tasks). Defaults produce the plain baseline
         build; native.pgoArg defaults to a harmless repeat of a flag already passed statically,
         since native-image doesn't tolerate an empty buildArg. -->
    <native.imageName>dotfile-java-tui</native.imageName>
    <native.pgoArg>--enable-native-access=ALL-UNNAMED</native.pgoArg>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>dev.tamboui</groupId>
        <artifactId>tamboui-bom</artifactId>
        <version>${tamboui.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jackson</artifactId>
    </dependency>

    <!-- TamboUI: Toolkit DSL (the fluent API — pulls widgets/core transitively)
         + Panama FFM terminal backend -->
    <dependency>
      <groupId>dev.tamboui</groupId>
      <artifactId>tamboui-toolkit</artifactId>
    </dependency>
    <dependency>
      <groupId>dev.tamboui</groupId>
      <artifactId>tamboui-panama-backend</artifactId>
    </dependency>

    <!-- Lombok: Boot-managed version; compile-time only (excluded from the jar) -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- JSpecify: @Nullable/@NonNull/@NullMarked annotations for compile-time null-safety
         (IDE + static-analysis tooling only — no runtime behavior) -->
    <dependency>
      <groupId>org.jspecify</groupId>
      <artifactId>jspecify</artifactId>
      <version>1.0.0</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <jvmArguments>--enable-native-access=ALL-UNNAMED</jvmArguments>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- Native-image build. Keeping this profile's id as "native" is required, not cosmetic:
         spring-boot-starter-parent ships its own built-in profile with this exact id that wires
         spring-boot:process-aot and auto-detects mainClass for native-maven-plugin; Maven merges a
         child profile into a parent profile of the same id, which is the only reason this works
         without us ever declaring a mainClass ourselves. An earlier attempt split this into separate
         native-instrument/native-pgo profile ids for the PGO workflow below and both failed with
         "Please specify class... containing the main entry point": neither inherited the parent's
         process-aot/mainClass wiring since their ids didn't match. Fixed by keeping one "native"
         profile, parametrized via command-line-overridable properties instead. mise run native is
         the baseline build (target/dotfile-java-tui.exe, GraalVM's default ML-inferred PGO); mise run
         native-instrument builds a profiling exe (target/dotfile-java-tui-sampling.exe); mise run
         native-pgo consumes the profile it collects to rebuild target/dotfile-java-tui.exe.
         native-instrument uses the pgo-sampling flag, not pgo-instrument, deliberately: pgo-instrument
         hit a genuine GraalVM 25.0.3 native-image compiler crash on this project (a LIR register-
         allocator assertion, "mismatched definition: r10|QWORD != r10|QWORD[.]", inside
         UpcallStubsHolder's upcall stub during method compilation) every time, apparently from
         pgo-instrument's counter-injection colliding with tamboui-panama-backend's one registered FFM
         upcall. pgo-sampling builds and runs cleanly, still writes a usable default.iprof, and is
         GraalVM's own documented lower-overhead alternative for exactly this kind of profiling — see
         FEATURE-PARITY.md for the full crash trace and how this was diagnosed. -->
    <profile>
      <id>native</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.graalvm.buildtools</groupId>
            <artifactId>native-maven-plugin</artifactId>
            <configuration>
              <imageName>${native.imageName}</imageName>
              <buildArgs>
                <buildArg>--enable-native-access=ALL-UNNAMED</buildArg>
                <!-- tamboui-panama-backend's WindowsTerminal uses a shared Arena for its FFM downcalls;
                     -H:+SharedArenaSupport is still experimental on GraalVM 25, hence the unlock flag -->
                <buildArg>-H:+UnlockExperimentalVMOptions</buildArg>
                <buildArg>-H:+SharedArenaSupport</buildArg>
                <buildArg>${native.pgoArg}</buildArg>
              </buildArgs>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
